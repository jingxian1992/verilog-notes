1.  线网（net）表示硬件单元之间的连接。
2.  线网一般使用关键字wire来声明。如果没有显式地
说明为向量，则默认线网的位宽为1.
3.  wire这个术语经常和net(线网)互换使用。
4.  线网的默认值为z（trireg除外，其默认值为x）。线网
的值由其驱动源决定。如果没有驱动源，则线网的值为z。
5.  注意，net不是一个关键字，它代表了一组数据类型，包括wire，
wand，wor，tri，triand，triort和trireg等。其中，wire最为常用。